<tr>
  <td><%= fa_icon event.icon, style: "font-size: 1.4em;" %> 
		<%= event.created_at.strftime("%H:%M") %>
		<%= event.name %> (<%= time_ago_in_words event.created_at %> ago)</td>
  <td><%= link_to fa_icon("close"), event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
